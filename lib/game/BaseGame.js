'use strict';

var log = require('./../log');
var Bytes = require('./../Bytes');
var _ = require('lodash');

function BaseGame(map, saveGame, hostPort, gameState, gameName, ownerName, creatorName, creatorServer) {
}

module.exports = BaseGame;
